### Getting Started

`git clone git@github.com:pmpurcell/django_practice.git`

Once the repo is cloned down, run this command in your terminal.

`python manage.py runserver`

This will open a local development server on the Polls application.

`{serverIP}/admin` <-- Will direct to the Admin site, where a user can add polls and questions.
